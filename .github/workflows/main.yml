on:
  push:
    branches:
      - master





jobs:
  build:
    runs-on: ubuntu-latest


    steps:
##
      - uses: actions/checkout@v3
      - name: 'Login to Docker hub'
        run: docker login --username boodyshlby55 --password boody91548180
      - name: 'Building Docker image'
        run: docker build -t boodyshlby55/myimage .
      - name: 'pushing to Docker hub'
        run: docker push boodyshlby55/myimage .
